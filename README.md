Day 1 Progress:

Infrastructure: Initialized Next.js + Supabase. Configured Google Cloud Console for OAuth.

Database: Created bookmarks table and enabled Row Level Security (RLS) to ensure users can only access their own data.

UI: Built the responsive login screen and authenticated "Home" view.

Challenge Faced: shifting from a traditional "Backend API" mindset to Supabase's "Client-side + RLS" architecture. I had to learn how to trust the database policies rather than writing manual middleware for every request.